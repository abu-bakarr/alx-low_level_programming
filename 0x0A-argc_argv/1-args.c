#include "holberton.h"
#include <stdio.h>

/**
 * main - function for this project.
 * @argc: is the length
 * @argv: string that holds statement.
 * Return: Null/Zero.
 */

int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", (argc - 1));
	return (0);
}
